[project]
name = "10-yolo26-yolo11-comparison"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "ultralytics",
    "pyautogui",       # for screen resolution
    "pyside6",
    "deep-translator"  # Google translate
]
